High
* fix compile warnings
* return errors on rewriting failure (no asserts)
* add much more test cases (for emulation of each instruction)
* improve instruction coverage
* use original instruction (inline asm) for emulation

Medium
* plugin interface (LLVM-IR route via plugin)
* better fallbacks
